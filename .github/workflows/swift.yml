# File: .github/workflows/swift.yml

name: Swift Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Swift Package
        run: swift build

      - name: Run Tests
        run: swift test
